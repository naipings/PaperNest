# 行为规则
1. 不要假设。不要隐藏困惑。主动暴露权衡取舍。
2. 只写解决当前问题的最小代码，不做任何推测性功能。
3. 只修改必须改的地方，只清理自己产生的问题。
4. 明确定义成功标准，验证通过前持续迭代。

## 反过度工程/反防御性编程
 - 信任内部代码和框架保证。
 - 只在系统边界（用户输入、外部API、网络）做校验。
 - 禁止为「不可能发生」的场景添加错误处理、回退、空值检查或验证。
 - 绝不吞掉错误（禁止 rescue nil/宽泛 catch/静默默认值）。
 - 禁止为一次性操作创建辅助函数、工具类或抽象。
 - 优先快速失败，而不是掩盖问题。

## 随时反思：
1. 你是不是又要过度工程化了？
2. 用一个会失败的单元测试证明它有必要。证明不了就别加。
3. 这个场景什么时候才会真正发生？
4. 有没有现成的知名开源库可以解决？
5. 别瞎猜，去看库的源码确认。

## 项目及文档维护
1. 维护产品开发需求和项目架构文档，后续动作都参考并实时更新它们。
2. 分阶段分功能逐步实现，不要一下子生成太多代码，并且每完成一部分功能后发布一个小版本（git提交），方便回退。
3. 完善测试流程，每部分功能实现单独测试模块，根据测试结果进行优化。

# 写作场景的反防御规则（撰写技术文档、调研方案、论文初稿场景下）
1. 直接陈述贡献，不要先写一堆「本文不声称……」。
2. 必要限制放到合适位置，而不是开头堆免责声明。
3. 禁止使用「值得注意的是」、「需要指出的是」、「可能」、「或许」、「潜在」这类默认犹豫词。
4. 删除一切不增加准确性的自我保护语句。
